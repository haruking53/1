using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test240422 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        aaa
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
